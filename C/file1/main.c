#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,sum; //khai báo biến
    scanf("%d%d",&a,&b); //nhập vào 2 số nguyên
    sum = a+b; //tong a va b
    printf("%d + %d = %d",a,b,sum);
    return 0;
}
